{
  "name": "nodejstemplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "start lite-server",
    "api": "start hai-server --watch database.json --port 8000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.3"
  },
  "devDependencies": {
    "hai-server": "^0.0.4",
    "lite-server": "^2.6.1"
  }
}
